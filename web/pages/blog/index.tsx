import Blogs from '../../components/Blogs';
import Layout from '../../components/Layout';

const BlogsPage = () => {
  return (
    <Layout>
      <Blogs />
    </Layout>
  );
};

export default BlogsPage;
